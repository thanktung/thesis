
\section{Xây dựng hệ thống xử lý ảnh}

\subsection{Pipeline xử lý ảnh}
Hệ thống kiểm tra ngoại quan tự động AVI vận hành thông qua một chuỗi các bước xử lý ảnh được tổ chức theo dạng pipeline nhằm đảm bảo quá trình kiểm tra diễn ra chính xác, ổn định và nhất quán. Pipeline xử lý ảnh của hệ thống có thể được mô tả qua các giai đoạn chính như sau:

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=2.4cm,
    box/.style={
        rectangle,
        draw,
        minimum width=3.5cm,
        minimum height=1.3cm,
        rounded corners=2pt,
        font=\small
    },
    arrow/.style={->, thick}
]

\node[box] (cap) {Capture};
\node[box, right=of cap] (pre) {Preprocessing};
\node[box, right=of pre] (align) {Alignment};

\node[box, below=2cm of align] (ana) {Analyzing};
\node[box, left=of ana] (dec) {Decision};
\node[box, left=of dec] (store) {Store / Log};

\draw[arrow] (cap) -- (pre);
\draw[arrow] (pre) -- (align);
\draw[arrow] (align) -- (ana);
\draw[arrow] (ana) -- (dec);
\draw[arrow] (dec) -- (store);

\end{tikzpicture}
\caption{Pipeline xử lý ảnh trong hệ thống AVI}
\label{fig:pipeline}
\end{figure}




\begin{enumerate}
    \item \textbf{Thu nhận hình ảnh (Capture)}  
    Ở giai đoạn đầu tiên, camera công nghiệp Basler acA2500 được sử dụng để thu nhận ảnh của PCB với độ phân giải cao.  
    Chất lượng ảnh đầu vào phụ thuộc vào:
    \begin{itemize}
        \item loại camera và ống kính,
        \item tốc độ màn trập, thời gian phơi sáng (Exposure),
        \item độ khuếch đại tín hiệu (Gain),
        \item cấu hình ánh sáng và góc chiếu.
    \end{itemize}

Mã nguồn Python thu ảnh từ Basler (pypylon) - Phụ lục \ref{sec:capture}.

Ảnh gốc từ camera:

    \begin{figure}[H]
        \centering
        \includegraphics[width=0.55\linewidth]{figures/Chapter4/i3.png}
        \caption{Ảnh PCB thu nhận từ camera}
    \end{figure}

% -------------------------------------------------------------

    \item \textbf{Xử lý và tiền xử lý hình ảnh (Preprocessing)}  
    Mục tiêu của bước này là cải thiện chất lượng ảnh để phục vụ cho xử lý phía sau.  
    Kỹ thuật áp dụng: CLAHE (Contrast Limited Adaptive Histogram Equalization)

    Mã nguồn Python cho code CLAHE - Phụ lục \ref{sec:clahe}.

    Kết quả sau khi xử lý bằng CLAHE như hình dưới:
    \begin{figure}[H]
        \centering
        \includegraphics[width=1\linewidth]{figures/Chapter4/i1.png}
        \caption{Ảnh sau khi xử lý bằng CLAHE}
    \end{figure}

% -------------------------------------------------------------

    \item \textbf{Alignment – Căn chỉnh ảnh so với ảnh mẫu (Golden Image)}  
    Đây là bước quan trọng nhằm đảm bảo sự trùng khớp giữa ảnh thực tế và ảnh chuẩn.

    Quá trình alignment gồm hai thành phần chính:

    \begin{itemize}
        \item \textbf{Trích xuất đặc trưng bằng ORB} (Oriented FAST and Rotated BRIEF)
        \item \textbf{Tìm ma trận biến đổi Homography} đưa ảnh input về đúng vị trí so với ảnh golden
        \item \textbf{Cắt ROI dựa trên toạ độ quy định trước}
    \end{itemize}



    \begin{figure}[H]
        \centering
        \includegraphics[width=0.8\linewidth]{figures/Chapter4/i4.png}
        \includegraphics[width=0.8\linewidth]{figures/Chapter4/i5.png}
        \caption{Ảnh sau căn chỉnh bằng thuật toán ORB + Homography}
    \end{figure}

    \begin{figure}[H]
        \centering
        \fbox{\includegraphics[width=0.8\linewidth]{figures/Chapter4/i6.png}}
        \caption{Ảnh sau khi cắt theo ảnh Golden}
    \end{figure}
% -------------------------------------------------------------

    \item \textbf{Analyzing – Phân tích ảnh}  
    Đây là bước cốt lõi của hệ thống, gồm hai quy trình:

    \begin{itemize}
        \item \textbf{Phát hiện linh kiện SMD bằng YOLO}  
        Mô hình YOLO được huấn luyện để nhận diện linh kiện (OK, Missing, Shifted, Wrong\_Orientation).
        \begin{figure}[H]
        \centering
        \begin{tikzpicture}
        \node[draw=black, line width=0.7pt] {
            \includegraphics[width=0.8\linewidth]{figures/Chapter4/i8.png}
        };
        \end{tikzpicture}
        \caption{Kết quả detect bằng YOLO}
        \end{figure}


    \end{itemize}


% -------------------------------------------------------------

    \item \textbf{Decision – Ra quyết định OK / NG}  
    Hệ thống dựa trên các tiêu chí:

    \begin{itemize}
        \item có đủ linh kiện hay không, ảnh ví dụ:
        \item vị trí có sai không,
    \end{itemize}
    \begin{figure}[h]
        \fbox{\includegraphics[width=0.48\linewidth]{figures/Chapter4/i9.png}
        \includegraphics[width=0.48\linewidth]{figures/Chapter4/i10.png}}
        \caption{Lỗi thiếu linh kiện và sai vị trí linh kiện}
            
    \end{figure}


% -------------------------------------------------------------

    \item \textbf{Store / Log – Lưu trữ kết quả}
    Gồm các thành phần:
    \begin{itemize}
        \item lưu ảnh gốc,
        \item lưu ảnh đã vẽ bounding box,
        \item lưu ảnh kết quả
    \end{itemize}

    
\end{enumerate}

Pipeline này giúp hệ thống AVI hoạt động mạch lạc, chính xác và có khả năng mở rộng theo yêu cầu sản xuất.

\subsection{Train mô hình YOLO detect linh kiện SMD}

\subsubsection{Thu thập dữ liệu}
Ảnh được thu thập trực tiếp từ camera trong dây chuyền bằng cách chụp nhiều mẫu PCB ở nhiều điều kiện khác nhau: thay đổi độ sáng, vị trí, góc đặt bảng, và trạng thái linh kiện (đầy đủ hoặc lỗi). Mục tiêu là tạo bộ dữ liệu đa dạng, phản ánh đúng môi trường sản xuất thực tế để mô hình học sâu có khả năng tổng quát tốt.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{figures/Chapter3/image11.png}
    \caption{Thu thập ảnh}
    \label{c3:acquistion}
\end{figure}

\subsubsection{Gán nhãn dữ liệu (Labeling)}
Sau khi thu thập, toàn bộ ảnh được đưa vào nền tảng Roboflow để thực hiện gán nhãn. Mỗi linh kiện SMD hoặc vùng lỗi trên PCB được bao quanh bằng bounding box và gán class tương ứng, chẳng hạn như \texttt{OK}, \texttt{Missing}, \texttt{Shifted}, \texttt{Wrong\_Orientation}, \ldots{} Việc gán nhãn được thực hiện thủ công nhằm đảm bảo độ chính xác, vì chất lượng nhãn có ảnh hưởng trực tiếp đến khả năng học của mô hình YOLO.

Roboflow hỗ trợ việc chuẩn hoá kích thước ảnh, tăng cường dữ liệu (augmentation) như xoay, thay đổi độ sáng, cắt, lật ảnh, giúp tăng độ đa dạng cho tập huấn luyện. Sau khi hoàn tất, bộ dữ liệu được export theo định dạng YOLO và sẵn sàng cho quá trình huấn luyện.
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{figures/Chapter3/image12.png}
    \caption{Label ảnh bằng Roboflow}
    \label{c3:labeling}
\end{figure}

\subsubsection{Huấn luyện mô hình (Train model)}
Bộ dữ liệu sau khi export được đưa lên môi trường Google Colab để huấn luyện mô hình YOLO. Colab cung cấp GPU miễn phí, phù hợp cho huấn luyện nhanh các mô hình kích thước vừa.

Quy trình huấn luyện gồm các bước:
\begin{itemize}
    \item Upload bộ dữ liệu từ Roboflow vào Colab bằng API.
    \item Cấu hình file huấn luyện: đường dẫn dữ liệu, số lớp (classes), độ phân giải ảnh, batch size, và số epochs.
    \item Tiến hành huấn luyện và theo dõi các chỉ số: loss, precision, recall, mAP.
    \item Lưu lại \texttt{best.pt} sau khi huấn luyện, sử dụng cho giai đoạn suy luận (inference) trong hệ thống AVI.
\end{itemize}

Mô hình sau khi huấn luyện được tải xuống và đưa vào pipeline xử lý ảnh để thực hiện phát hiện linh kiện SMD trong thời gian gần thực.

